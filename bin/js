#!/usr/bin/node

const readline = require("readline");

const rl = readline.createInterface({
  input: process.stdin,
  output: process.stdout,
  terminal: false
});

const args = [];
const flags = [];
process.argv.slice(2).forEach(arg => {
  if (arg.startsWith("-")) {
    flags.push(arg.replace(/^--?/, ""));
  } else {
    args.push(arg);
  }
});

const userFn = Function(
  "stdin_QtZSMvEPLu",
  `return stdin_QtZSMvEPLu.${args[0]}`
);

rl.on("line", stdin => {
  const res = userFn(stdin);
  if (res != undefined || flags.includes("a") || flags.includes("all")) {
    console.log(res);
  }
  //process.stdout.write(userFn(stdin).toString() + "\n")
});
